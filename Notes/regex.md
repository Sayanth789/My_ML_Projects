The expressions (?<!\S) and (?!\S) are regex **negative lookbehind** and **negative lookahead** assertions that 
ensure a **bigram** stands alone....
The likelihood checks that no non-whitespace character preceds the **bigram** menaning it follows whitespace or the start
of the text.
